### logic to load parameters from file, convert them to usable standardized quantities and units and package them into dicts

import yaml

def load_constants( path: str = "constants.yml" ) -> dict:
    pass

def load_parameters( path: str = "parameters.yml" ) -> dict:
    pass

